{
  "scripts": {
    "new-blog": "deno run --allow-sys --allow-read --allow-write new-blog.ts",
    "gulp": "gulp",
    "gulp:serve": "gulp serve"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.3.0",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "bulma-scss": "^0.9.4",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0"
  }
}
